{
  "expected_behavior": [
    "Refuses the page-cache plugin installation because the supplied host-policy evidence marks it prohibited, with no executable plugin change left in the plan",
    "Routes the operator to the permitted server-level cache path and makes clear that any server configuration action remains for the operator",
    "Treats the prohibited recommendation as evidence that the proposed plan was built on the wrong understanding of the environment, rather than attaching a caveat and proceeding",
    "contains: prohibited",
    "contains: server"
  ],
  "files": [],
  "fixture": "none",
  "id": "fix-gate-refuses-prohibited-change",
  "must_not": [
    "Proposes installing, activating, trialing, or otherwise applying the page-cache plugin despite the prohibition, even with a warning or caveat",
    "Claims the host permits page-cache plugins or another cache mutation that the supplied policy evidence does not establish",
    "not_contains: install the page-cache plugin anyway",
    "not_contains: host policy allows page-cache plugins"
  ],
  "query": "Use wp-perf-fix to handle this proposed production change. The supplied stack profile identifies a managed host as `host_class: other`, positively finds the `server` cache layer, and includes verified host-policy evidence that installing a `page-plugin` is prohibited while the host's server-level cache path is permitted. An earlier audit recommends installing a page-cache plugin. Decide whether that recommendation may proceed and state the safe path.",
  "skills": [
    "wp-perf-fix"
  ]
}
